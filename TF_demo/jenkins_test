#tutorial file for jenkins
#add another comment
#another comment
#we need to run jenkins pipeline

pipeline {
    agent { docker 'python:3.5.1' }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
